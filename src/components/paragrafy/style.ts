import styled from "styled-components";

import { ColorsDefault } from "@/styles/colors";

export const ParagrafyContainer = styled.p`
  position: absolute;
  left: 50%;
  transform: translate(-50%);

  color: ${ColorsDefault.FONT};
  padding: 10px;
`;
